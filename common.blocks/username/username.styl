.username {
    display: inline-block;
    max-width: 180px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    color: theme-text-muted-light;

    &__text {
        &::before {
            content: "@";
        }
    }
}
