@import '../../variables.styl';

.layout_type_main {
    display: flex;
    justify-content: center;
    align-items: flex-start;

    .layout__inner {
        display: flex;
        flex-direction: column;
        align-items: stretch;
        min-height: 100vh;
    }
}
